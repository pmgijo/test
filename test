import React, { useState } from "react";

const Income = (props) => {
  const [applicants, setApplicants] = useState([{}, {}, {}, {}, {}, {}]);
  return (
    <div>
      <table>
        <thead>
          <tr>
            <th>Personal Income Individual</th>
            <th colSpan={applicants.length}>
              <div style={{ overflowX: 'auto', whiteSpace: 'nowrap', maxWidth: '500px' }}>
                <table>
                  <thead>
                    <tr>
                      {applicants?.map((appl, index) => (
                        <th key={index} style={{ minWidth: '100px' }}>Applicant {index + 1}</th>
                      ))}
                    </tr>
                  </thead>
                </table>
              </div>
            </th>
            <th>Total</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>Salary/Wages</td>
            <td colSpan={applicants.length}>
              <div style={{ overflowX: 'auto', whiteSpace: 'nowrap', maxWidth: '500px' }}>
                <table>
                  <tbody>
                    <tr>
                      {applicants?.map((appl, index) => (
                        <td key={index} style={{ minWidth: '100px' }}>1212.00</td>
                      ))}
                    </tr>
                  </tbody>
                </table>
              </div>
            </td>
            <td>687678.00</td>
          </tr>
        </tbody>
      </table>
    </div>
  );
};

export default Income;
